<!-- <section id="why-me" class="bg-primary flex items-center justify-center p-8">
  <div class="flex gap-4">
    <div
      class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg w-4xl"
      data-aos="fade-up"
      data-aos-delay="0"
    >
      <img src="/images/briefcase.svg" class="w-12 sm:w-14" />
      <div class="font-bold text-white">
        <h2 class="text-2xl md:text-3xl">Experiencia que se nota</h2>
        <p class="mt-2 text-sm md:text-base">
          Llevo años creando y perfeccionando sitios web. Se lo que funciona y
          cómo hacerlo brillar.
        </p>
      </div>
    </div>

    <div
      class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg w-4xl"
      data-aos="fade-up"
      data-aos-delay="100"
    >
      <img src="/images/artist-palette.svg" class="w-12 sm:w-14" />
      <div class="font-bold text-white">
        <h2 class="text-2xl md:text-3xl">Creatividad que te hace destacar</h2>
        <p class="mt-2 text-sm md:text-base">
          La clave para diferenciarte es ofrecer experiencias únicas que
          conecten con tu audiencia y dejen huella.
        </p>
      </div>
    </div>

    <div
      class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg w-4xl"
      data-aos="fade-up"
      data-aos-delay="200"
    >
      <img src="/images/joker.svg" class="w-12 sm:w-14" />
      <div class="font-bold text-white">
        <h2 class="text-2xl md:text-3xl">Flexibilidad total</h2>
        <p class="mt-2 text-sm md:text-base">
          Me adapto a vos y a lo que tu proyecto necesita, porque cada cliente
          es único.
        </p>
      </div>
    </div>

    <div
      class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg w-4xl"
      data-aos="fade-up"
      data-aos-delay="300"
    >
      <img src="/images/magnifying-glass.svg" class="w-12 sm:w-14" />
      <div class="font-bold text-white">
        <h2 class="text-2xl md:text-3xl">Atención al detalle</h2>
        <p class="mt-2 text-sm md:text-base">
          Los pequeños detalles marcan la diferencia. Me aseguro de que todo en
          tu proyecto sea perfecto.
        </p>
      </div>
    </div>

    <div
      class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg w-4xl"
      data-aos="fade-up"
      data-aos-delay="400"
    >
      <img src="/images/right-anger-bubble.svg" class="w-12 sm:w-14" />
      <div class="font-bold text-white">
        <h2 class="text-2xl md:text-3xl">Transparencia total</h2>
        <p class="mt-2 text-sm md:text-base">
          La comunicación fluida es el secreto para el éxito de cualquier
          proyecto.
        </p>
      </div>
    </div>
  </div>
</section> -->
<section
  id="why-me"
  class="bg-primary flex flex-col gap-20 overflow-hidden relative py-16"
>
  <div class="flex justify-center text-center">
    <h2
      data-aos="fade-up"
      class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white"
    >
      POR QUE TRABAJAR CONMIGO?
    </h2>
  </div>

  <div class="animate-scroll flex gap-6 w-max">
    {
      [
        "Experiencia que se nota",
        "Creatividad que te hace destacar",
        "Flexibilidad total",
        "Atención al detalle",
        "Transparencia total",
      ].map((title, i) => (
        <div
          class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg flex-shrink-0 w-[90vw] sm:w-auto sm:min-w-[350px]"
          data-aos="fade-up"
          data-aos-delay={i * 100}
        >
          <img
            src={`/images/${i === 0 ? "briefcase" : i === 1 ? "artist-palette" : i === 2 ? "joker" : i === 3 ? "magnifying-glass" : "right-anger-bubble"}.svg`}
            class="w-12 sm:w-14"
          />
          <div class="font-bold text-white">
            <h2 class="text-2xl md:text-3xl">{title}</h2>
            <p class="mt-2 text-sm md:text-base">
              {i === 0
                ? "Llevo años creando y perfeccionando sitios web. Se lo que funciona y cómo hacerlo brillar."
                : i === 1
                  ? "La clave para diferenciarte es ofrecer experiencias únicas que conecten con tu audiencia y dejen huella."
                  : i === 2
                    ? "Me adapto a vos y a lo que tu proyecto necesita, porque cada cliente es único."
                    : i === 3
                      ? "Los pequeños detalles marcan la diferencia. Me aseguro de que todo en tu proyecto sea perfecto."
                      : "La comunicación fluida es el secreto para el éxito de cualquier proyecto."}
            </p>
          </div>
        </div>
      ))
    }
    {
      [
        "Experiencia que se nota",
        "Creatividad que te hace destacar",
        "Flexibilidad total",
        "Atención al detalle",
        "Transparencia total",
      ].map((title, i) => (
        <div class="flex items-start bg-secondary rounded-xl p-6 gap-4 shadow-lg flex-shrink-0 w-[90vw] sm:w-auto sm:min-w-[350px]">
          <img
            src={`/images/${i === 0 ? "briefcase" : i === 1 ? "artist-palette" : i === 2 ? "joker" : i === 3 ? "magnifying-glass" : "right-anger-bubble"}.svg`}
            class="w-12 sm:w-14"
          />
          <div class="font-bold text-white">
            <h2 class="text-2xl md:text-3xl">{title}</h2>
            <p class="mt-2 text-sm md:text-base">
              {i === 0
                ? "Llevo años creando y perfeccionando sitios web. Se lo que funciona y cómo hacerlo brillar."
                : i === 1
                  ? "La clave para diferenciarte es ofrecer experiencias únicas que conecten con tu audiencia y dejen huella."
                  : i === 2
                    ? "Me adapto a vos y a lo que tu proyecto necesita, porque cada cliente es único."
                    : i === 3
                      ? "Los pequeños detalles marcan la diferencia. Me aseguro de que todo en tu proyecto sea perfecto."
                      : "La comunicación fluida es el secreto para el éxito de cualquier proyecto."}
            </p>
          </div>
        </div>
      ))
    }
  </div>

  <div
    data-aos="fade-up"
    class="flex flex-col justify-center items-center text-center"
  >
    <h2 class="text-3xl sm:text-4xl md:text-5xl font-extrabold text-white">
      ¡DALE! MANDAME UN MENSAJE Y ARMEMOS ALGO COPADO.
    </h2>
    <img src="/images/smiling-face-with-sunglasses.svg" class="w-12 sm:w-14" />
  </div>

  <style is:global>
    @keyframes scroll {
      0% {
        transform: translateX(0);
      }
      100% {
        transform: translateX(-50%);
      }
    }
    .animate-scroll {
      display: flex;
      animation: scroll 40s linear infinite;
    }
  </style>
</section>
